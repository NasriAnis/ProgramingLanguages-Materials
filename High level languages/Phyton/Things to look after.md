1. Ternary operators
2. The break and pass statement in conditional and loops + logical operators 
3. `.keys()` `.values()` and `.items()` in for loops
4. Built in python functions
5. `.enumerate` built in function
6. `return` function
7. Python’s built-in functions [`all()`]()`), [`any()`]()`), [`hasattr()`]()`), [`isinstance()`]()`), and [`issubclass()`]()`)
8. `as` keyword
9. functional tools (a few other built-in and standard-library functions that allow you to traverse a list of values and obtain a final result either as another list, an iterator, or even a single value. Some examples include [`reduce()`]()`), [`min(|[)` and `max()`]()`)` and `max()`]], [`sum()`]()`), [`all()`]()`), and [`any()`]()`).)
10. IMPORTANT # Subclassing the Built-In `list` Class https://realpython.com/python-list/#subclassing-the-built-in-list-class
11. class and .slots