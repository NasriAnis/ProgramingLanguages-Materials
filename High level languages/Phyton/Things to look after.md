1. Ternary operators
2. The break and pass statement in conditional and loops + logical operators 
3. `.keys()` `.values()` and `.items()` in for loops
4. Built in python functions
5. `.enumerate` built in function
6. `return` function
7. Python’s built-in functions [[)`](https://realpython.com/python-all/|`all()`]], [[)`](https://realpython.com/any-python/|`any()`]], [[)`](https://realpython.com/ref/builtin-functions/hasattr/|`hasattr()`]], [[)`](https://realpython.com/ref/builtin-functions/isinstance/|`isinstance()`]], and [[)`](https://realpython.com/ref/builtin-functions/issubclass/|`issubclass()`]]
8. `as` keyword
9. functional tools (a few other built-in and standard-library functions that allow you to traverse a list of values and obtain a final result either as another list, an iterator, or even a single value. Some examples include [[)`](https://realpython.com/python-reduce-function/|`reduce()`]], [[)`](https://realpython.com/python-min-and-max/|`min(|[)` and `max()`]]` and `max()`]], [[)`](https://realpython.com/python-sum-function/|`sum()`]], [[)`](https://realpython.com/python-all/|`all()`]], and [[)`](https://realpython.com/any-python/|`any()`]].)
10. IMPORTANT # Subclassing the Built-In `list` Class https://realpython.com/python-list/#subclassing-the-built-in-list-class
11. class and .slots