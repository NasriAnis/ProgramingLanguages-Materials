# Math relates function :
- round() : to round a value
- abs() : gives the absolute value of a number
- pow(x, y) : gives the result of x to the power of y
- max (x, y, z, ...) : gives the bigger value between the variables
- min(x, ...) : ~
# Arithmetic operators :
- +
- -
- x
- /
- x** y
- %
# Constant and functions :
Always add ==import math== at the top of the code to use these constant and functions.
- math.Pi
- math.sqrt() : square root
- math.ceil() : round up
- math.floor(variable, ...) : round down
-... round up to how many digits